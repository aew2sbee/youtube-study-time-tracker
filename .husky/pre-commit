#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running lint..."
npm run lint || { echo "❌ Lint failed. Please fix the errors before committing."; exit 1; }

echo "🔨 Running build..."
npm run build || { echo "❌ Build failed. Please fix the errors before pushing."; exit 1; }

echo "✅ Lint passed!"
